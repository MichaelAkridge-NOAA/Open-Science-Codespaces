{
  // Name of the Dev Container
  "name": "RStudio Codespace - Example 02",

  // Image to use for the container
  "image": "rocker/rstudio:latest",

  // Default workspace folder within the container
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  // Settings for the integrated terminal
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  // Ports to be forwarded from the container to the host
  "forwardPorts": [8787],

  // Attributes for the forwarded ports
  "portsAttributes": {
    "8787": {
      "label": "rstudio",
      "onAutoForward": "openPreview"
    }
  },

  // Command to run after the container starts. Runs script to add a link to the RStudio
  "postStartCommand": "chmod +x /workspaces/${localWorkspaceFolderBasename}/workspace/post_start.sh && /workspaces/${localWorkspaceFolderBasename}/workspace/post_start.sh",

  // Mounting volumes
  "mounts": [
    "source=${localWorkspaceFolder}/.rstudio_config,target=/home/rstudio/.config/rstudio,type=bind",
    "source=${localWorkspaceFolder}/workspace,target=/workspaces/${localWorkspaceFolderBasename}/workspace,type=bind",
    "source=${localWorkspaceFolder}/other_dir,target=/workspaces/${localWorkspaceFolderBasename}/other_dir,type=bind"
  ],

  // Environment variables
  "remoteEnv": {
    "DISABLE_AUTH": "true"
  }
}

