{
  // Name of the Dev Container
  "name": "RStudio Codespace - Example 02",

  // Image to use for the container
  "image": "rocker/rstudio:latest",

  // Default workspace folder within the container
  "workspaceFolder": "/workspaces/rstudio-example-02/workspace",

  // Settings for the integrated terminal
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  // Ports to be forwarded from the container to the host
  "forwardPorts": [8787],

  // Attributes for the forwarded ports
  "portsAttributes": {
    "8787": {
      "label": "rstudio",
      "onAutoForward": "openPreview"
    }
  },

  // Command to run after the container starts. Runs script to add a link to the RStudio
  "postStartCommand": "chmod +x /workspaces/rstudio-example-02/workspace/post_start.sh && /workspaces/rstudio-example-02/workspace/post_start.sh",

  // Mounting volumes
  "mounts": [
    "source=${localWorkspaceFolder}/rstudio-example-02/.rstudio_config,target=/home/rstudio/.config/rstudio,type=bind",
    "source=${localWorkspaceFolder}/rstudio-example-02/workspace,target=/workspaces/rstudio-example-02/workspace,type=bind",
    "source=${localWorkspaceFolder}/rstudio-example-02/other_dir,target=/workspaces/rstudio-example-02/other_dir,type=bind"
  ],

  // Environment variables
  "remoteEnv": {
    "DISABLE_AUTH": "true"
  }
}

